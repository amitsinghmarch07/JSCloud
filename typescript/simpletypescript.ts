let message: string;
message = "welcome to typescript";

// message = "simple typescript";
console.log(message);
function addNumber(a: number, b: number): number {
    return a + b;
}

addNumber(5, 10);