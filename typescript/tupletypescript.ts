const array: number[] = [];
// array. push ("abc");
array.push(1);
console.log(array[o]);
// let num: readonly number = 1;
const lang: readonly string[]
["Python", "JavaScript"];
// lang.push (1);
console.log(lang[o]);
let tuple: [number, string, boolean];
tuple = [2, "Brendan", true];
// tuple.push(59); //~! Unfortunate downside
console.log(tuple[0]);
let fixed: readonly [number, string];
fixed = [1, "ABC"];
// fixed.push (59) ;
console.log(fixed);
const OPEN STATE E
0;
cost ERROR_STATE
1;
const CLOSED STATE
2;
enum State {
    Open,
    Error,
    Closed
}
let state: State;
state = State.Open;
state = State.Unknown;
state = 2;

